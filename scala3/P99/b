echo ""
if [ $# -eq 0 ]; then	
 	for srcfile in src/*.scala; do 
		echo $srcfile
		scala $srcfile 
	done 
	set srcfiles=src/*.scala
	scala repl <<EOF
	:load	$srcfiles
EOF
echo ""
else
#cat $1 |scala repl
scala $1
fi

echo ""
